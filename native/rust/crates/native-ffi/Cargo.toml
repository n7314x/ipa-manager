[package]
name = "native-ffi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
device-core = { path = "../device-core" }
ipa-inspect = { path = "../ipa-inspect" }
